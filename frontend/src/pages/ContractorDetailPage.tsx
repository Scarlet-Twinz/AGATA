import { useEffect, useMemo, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { api } from "../lib/api";

type Contractor = { id: string; name: string; email: string | null; phone: string | null; status: string; created_at: string };
type Project = { id: string; name: string; description: string | null; status: string };
type ContractorDetail = { contractor: Contractor; documents: { id: string; name: string; document_type: string; expires_at: string | null; status: string }[]; projects: { id: string; name: string; status: string; readiness: { score: number; status: string; checked_at?: string; explanation?: string } | null }[] };

function Icon({ name, size = 18 }: { name: "back" | "plus" | "folder" | "file" | "mail" | "phone" | "close" | "arrow" | "check"; size?: number }) {
  const c = { width: size, height: size, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.7, strokeLinecap: "round" as const, strokeLinejoin: "round" as const };
  if (name === "back") return <svg {...c}><path d="M19 12H6"/><path d="m11 6-6 6 6 6"/></svg>;
  if (name === "plus") return <svg {...c}><path d="M12 5v14"/><path d="M5 12h14"/></svg>;
  if (name === "folder") return <svg {...c}><path d="M3.5 6.5A2.5 2.5 0 0 1 6 4h4l2 2h6a2.5 2.5 0 0 1 2.5 2.5v9A2.5 2.5 0 0 1 18 20H6a2.5 2.5 0 0 1-2.5-2.5v-11Z"/></svg>;
  if (name === "file") return <svg {...c}><path d="M6 3h8l4 4v14H6z"/><path d="M14 3v5h5"/><path d="M9 13h6"/></svg>;
  if (name === "mail") return <svg {...c}><rect x="3" y="5" width="18" height="14" rx="2"/><path d="m4 7 8 6 8-6"/></svg>;
  if (name === "phone") return <svg {...c}><path d="M7 3.5 10 5l-1.5 3.5a13 13 0 0 0 4.5 4.5L16.5 11l3 3-.8 3.1a2 2 0 0 1-2 1.5C10 18 6 14 4.4 7.3a2 2 0 0 1 1.5-2L7 3.5Z"/></svg>;
  if (name === "close") return <svg {...c}><path d="m7 7 10 10"/><path d="m17 7-10 10"/></svg>;
  if (name === "check") return <svg {...c}><path d="m5 12 4 4L19 6"/></svg>;
  return <svg {...c}><path d="M5 12h13"/><path d="m13 6 6 6-6 6"/></svg>;
}

function readinessLabel(value: string | null) { return value === "ready" ? "Ready" : value === "attention" ? "Attention" : value === "not_ready" ? "Not ready" : "Not evaluated"; }
function dateLabel(value: string | null) { if (!value) return "No expiry"; const date = new Date(value); return Number.isNaN(date.getTime()) ? "Unknown" : date.toLocaleDateString(undefined, { day: "numeric", month: "short", year: "numeric" }); }
function expiryTone(value: string | null) { if (!value) return "valid"; const time = new Date(value).getTime(); if (Number.isNaN(time)) return "valid"; const days = (time - Date.now()) / 86400000; return days < 0 ? "expired" : days <= 30 ? "soon" : "valid"; }

export default function ContractorDetailPage() {
  const { contractorId } = useParams();
  const navigate = useNavigate();
  const [detail, setDetail] = useState<ContractorDetail | null>(null);
  const [projects, setProjects] = useState<Project[]>([]);
  const [loading, setLoading] = useState(true);
  const [busyId, setBusyId] = useState("");
  const [error, setError] = useState("");
  const [showAssign, setShowAssign] = useState(false);

  async function load() {
    if (!contractorId) return;
    try {
      setLoading(true); setError("");
      const [result, projectList] = await Promise.all([api<ContractorDetail>(`/api/contractors/${contractorId}`), api<Project[]>("/api/projects")]);
      setDetail(result); setProjects(projectList);
    } catch (err) { setError(err instanceof Error ? err.message : "Unable to load this contractor."); }
    finally { setLoading(false); }
  }
  useEffect(() => { void load(); }, [contractorId]);

  const assignedIds = useMemo(() => new Set((detail?.projects || []).map((project) => project.id)), [detail]);
  const availableProjects = projects.filter((project) => !assignedIds.has(project.id));
  const evaluated = detail?.projects.map((project) => project.readiness).filter(Boolean) as { score: number; status: string }[] | undefined;
  const readiness = evaluated?.length ? (evaluated.every((item) => item.status === "ready") ? "ready" : evaluated.some((item) => item.status === "not_ready") ? "not_ready" : "attention") : null;
  const averageScore = evaluated?.length ? Math.round(evaluated.reduce((sum, item) => sum + item.score, 0) / evaluated.length) : null;
  const soonCount = detail?.documents.filter((document) => expiryTone(document.expires_at) === "soon").length ?? 0;
  const expiredCount = detail?.documents.filter((document) => expiryTone(document.expires_at) === "expired").length ?? 0;

  async function assign(projectId: string) {
    if (!contractorId) return;
    try { setBusyId(projectId); setError(""); await api(`/api/projects/${projectId}/contractors`, { method: "POST", body: JSON.stringify({ contractor_id: contractorId }) }); setShowAssign(false); await load(); }
    catch (err) { setError(err instanceof Error ? err.message : "Unable to assign contractor."); }
    finally { setBusyId(""); }
  }

  async function remove(projectId: string) {
    if (!contractorId) return;
    try { setBusyId(projectId); setError(""); await api(`/api/projects/${projectId}/contractors/${contractorId}`, { method: "DELETE" }); await load(); }
    catch (err) { setError(err instanceof Error ? err.message : "Unable to remove project assignment."); }
    finally { setBusyId(""); }
  }

  if (loading) return <><style>{styles}</style><section className="contractor-detail-state"><div className="contractor-detail-spinner"/><span>Loading contractor workspace…</span></section></>;
  if (error && !detail) return <><style>{styles}</style><section className="contractor-detail-state"><strong>We couldn't load this contractor.</strong><p>{error}</p><Link to="/contractors" className="cd-secondary">Back to contractors</Link></section></>;
  if (!detail) return null;

  return <>
    <style>{styles}</style>
    <section className="contractor-detail-page">
      <div className="cd-breadcrumb"><Link to="/contractors"><Icon name="back" size={15}/> Contractors</Link><span>/</span><span>{detail.contractor.name}</span></div>
      <header className="cd-header"><div><span className="cd-eyebrow">CONTRACTOR PROFILE</span><div className="cd-title-line"><div className="cd-avatar">{detail.contractor.name.slice(0,1).toUpperCase()}</div><div><h1>{detail.contractor.name}</h1><span className={`cd-status ${detail.contractor.status === "active" ? "active" : "inactive"}`}>{detail.contractor.status === "active" ? "Active" : detail.contractor.status}</span></div></div><p>Evidence owner for assigned projects and readiness decisions.</p></div><Link to="/contractors" className="cd-secondary">All contractors</Link></header>
      {error && <div className="cd-error" role="alert">{error}</div>}
      <section className="cd-summary-grid"><article><span>Evidence</span><strong>{detail.documents.length}</strong><small>{soonCount || expiredCount ? `${soonCount} expiring · ${expiredCount} expired` : "No upcoming expiry issues"}</small></article><article><span>Projects</span><strong>{detail.projects.length}</strong><small>Assigned projects</small></article><article><span>Readiness</span><strong>{averageScore == null ? "—" : `${averageScore}%`}</strong><small>{readiness ? readinessLabel(readiness) : "No evaluated project"}</small></article><article><span>Contact</span><strong>{detail.contractor.email || detail.contractor.phone ? "Available" : "—"}</strong><small>{detail.contractor.email || detail.contractor.phone || "No contact details"}</small></article></section>

      <div className="cd-grid">
        <main className="cd-primary">
          <section className="cd-panel"><div className="cd-panel-head"><div><span className="cd-eyebrow">PROJECT ASSIGNMENTS</span><h2>Projects</h2><p>Only assigned projects are part of this contractor's readiness workflow.</p></div><button className="cd-primary-button" type="button" onClick={() => setShowAssign(true)}><Icon name="plus" size={15}/> Assign project</button></div>{detail.projects.length === 0 ? <div className="cd-empty"><div className="cd-empty-icon"><Icon name="folder" size={22}/></div><strong>No projects assigned</strong><span>Assign a project to begin evaluating this contractor against its requirements.</span><button className="cd-secondary" type="button" onClick={() => setShowAssign(true)}>Assign first project</button></div> : <div className="cd-project-list">{detail.projects.map((project) => <div className="cd-project-row" key={project.id}><div className="cd-project-icon"><Icon name="folder" size={15}/></div><div className="cd-project-info"><strong>{project.name}</strong><span>{project.readiness ? `${project.readiness.score}% · ${readinessLabel(project.readiness.status)}` : "Not evaluated"}</span></div><Link to={`/projects/${project.id}`} className="cd-row-link">Open <Icon name="arrow" size={13}/></Link><button className="cd-remove" type="button" disabled={busyId === project.id} onClick={() => void remove(project.id)}>{busyId === project.id ? "…" : "Remove"}</button></div>)}</div>}</section>

          <section className="cd-panel"><div className="cd-panel-head"><div><span className="cd-eyebrow">EVIDENCE</span><h2>Evidence</h2><p>Documents owned by this contractor and available to the readiness engine.</p></div><Link className="cd-text-link" to="/evidence">Open evidence <Icon name="arrow" size={13}/></Link></div>{detail.documents.length === 0 ? <div className="cd-empty compact"><div className="cd-empty-icon"><Icon name="file" size={22}/></div><strong>No evidence yet</strong><span>Evidence will appear here when it is added to this contractor.</span></div> : <div className="cd-doc-list">{detail.documents.map((document) => <div className="cd-doc-row" key={document.id}><div className="cd-doc-icon"><Icon name="file" size={15}/></div><div className="cd-doc-info"><strong>{document.name}</strong><span>{document.document_type}</span></div><span className={`cd-expiry ${expiryTone(document.expires_at)}`}>{dateLabel(document.expires_at)}</span></div>)}</div>}</section>
        </main>

        <aside className="cd-sidebar"><section className="cd-panel"><span className="cd-eyebrow">CONTACT</span><h2 className="cd-side-title">Contact details</h2><div className="cd-contact-list">{detail.contractor.email && <div><Icon name="mail" size={15}/><span>{detail.contractor.email}</span></div>}{detail.contractor.phone && <div><Icon name="phone" size={15}/><span>{detail.contractor.phone}</span></div>}{!detail.contractor.email && !detail.contractor.phone && <div className="cd-muted">No contact details provided.</div>}</div></section><section className="cd-panel"><span className="cd-eyebrow">READINESS</span><div className="cd-readiness-score">{averageScore == null ? "—" : `${averageScore}%`}</div><p className="cd-side-copy">{readiness ? `Across ${evaluated?.length} evaluated project${evaluated?.length === 1 ? "" : "s"}: ${readinessLabel(readiness)}.` : "Evaluate this contractor from an assigned project to generate readiness."}</p><Link to="/readiness" className="cd-wide-link">Open readiness <Icon name="arrow" size={13}/></Link></section></aside>
      </div>
    </section>

    {showAssign && <div className="cd-modal-backdrop" onMouseDown={(event) => { if (event.currentTarget === event.target) setShowAssign(false); }}><section className="cd-modal" role="dialog" aria-modal="true" aria-labelledby="assign-project-title"><div className="cd-modal-head"><div><span className="cd-eyebrow">PROJECT ASSIGNMENT</span><h2 id="assign-project-title">Assign a project</h2><p>Choose an existing project for this contractor.</p></div><button className="cd-close" type="button" onClick={() => setShowAssign(false)} aria-label="Close"><Icon name="close" size={17}/></button></div>{availableProjects.length === 0 ? <div className="cd-picker-empty"><strong>No available projects</strong><span>This contractor is already assigned to every project in the workspace.</span></div> : <div className="cd-picker-list">{availableProjects.map((project) => <button className="cd-picker-row" type="button" key={project.id} disabled={busyId === project.id} onClick={() => void assign(project.id)}><span className="cd-project-icon"><Icon name="folder" size={15}/></span><span><strong>{project.name}</strong><small>{project.status}</small></span><span className="cd-picker-arrow"><Icon name="arrow" size={14}/></span></button>)}</div>}</section></div>}
  </>;
}

const styles = `
.contractor-detail-page{max-width:1180px;margin:0 auto;padding:34px 30px 56px;color:#e8eef7}.cd-breadcrumb{display:flex;align-items:center;gap:8px;color:#526b83;font-size:10px;margin-bottom:22px}.cd-breadcrumb a{display:flex;align-items:center;gap:7px;color:#88a6bf;text-decoration:none}.cd-breadcrumb a:hover{color:#c2d6e7}.cd-header{display:flex;align-items:flex-end;justify-content:space-between;gap:28px;margin-bottom:26px}.cd-eyebrow{display:block;font-size:9px;letter-spacing:.19em;font-weight:700;color:#7f9bb8;margin-bottom:8px}.cd-title-line{display:flex;align-items:center;gap:14px}.cd-avatar{width:48px;height:48px;border:1px solid #2a4861;border-radius:13px;background:#081827;display:grid;place-items:center;color:#94b9d8;font-size:16px;font-weight:750}.cd-header h1{margin:0;color:#edf3f8;font-size:36px;line-height:1.05;letter-spacing:-.04em}.cd-header p{margin:11px 0 0;color:#718aa2;font-size:12px}.cd-status{display:inline-flex;margin-top:7px;padding:5px 8px;border-radius:999px;font-size:9px;font-weight:650}.cd-status.active{border:1px solid #284b65;background:#091c2d;color:#83afd2}.cd-status.inactive{border:1px solid #3a4047;background:#16191d;color:#89939e}.cd-secondary{border:1px solid #29455e;border-radius:10px;background:transparent;color:#a9bdd0;font:600 12px/1 inherit;padding:11px 14px;display:inline-flex;align-items:center;justify-content:center;gap:7px;text-decoration:none;cursor:pointer}.cd-secondary:hover{border-color:#3c6b91;background:#0a1725}.cd-error{border:1px solid #6b3941;background:#29171c;color:#e9b5bc;border-radius:10px;padding:11px 13px;margin-bottom:16px;font-size:12px}.cd-summary-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px;margin-bottom:18px}.cd-summary-grid article{min-height:104px;border:1px solid #1b3044;border-radius:13px;background:#07111b;padding:16px}.cd-summary-grid span{display:block;color:#637d95;font-size:10px}.cd-summary-grid strong{display:block;margin-top:8px;color:#e5edf5;font-size:24px;letter-spacing:-.04em}.cd-summary-grid small{display:block;margin-top:6px;color:#5e778f;font-size:9px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cd-grid{display:grid;grid-template-columns:minmax(0,1fr) 310px;gap:16px;align-items:stretch}.cd-primary{min-width:0;display:flex;flex-direction:column;gap:16px}.cd-sidebar{display:flex;flex-direction:column;gap:16px}.cd-panel{border:1px solid #1b3044;border-radius:14px;background:#050b13;padding:20px}.cd-sidebar .cd-panel{flex:1}.cd-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:16px}.cd-panel h2{margin:0;color:#e1eaf2;font-size:18px;letter-spacing:-.025em}.cd-panel-head p{margin:7px 0 0;color:#657f97;font-size:10px;max-width:650px}.cd-primary-button{border:1px solid #29455e;border-radius:10px;background:#0b1d2e;color:#dce9f7;font:600 11px/1 inherit;padding:10px 13px;display:inline-flex;align-items:center;justify-content:center;gap:7px;cursor:pointer;white-space:nowrap}.cd-primary-button:hover{border-color:#3c6b91;background:#10283d}.cd-empty{min-height:220px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:24px}.cd-empty.compact{min-height:170px}.cd-empty-icon{width:46px;height:46px;border:1px solid #28445c;border-radius:12px;background:#081827;display:grid;place-items:center;color:#79a6ce;margin-bottom:12px}.cd-empty strong{font-size:13px;color:#dce6ef}.cd-empty span{max-width:440px;margin:7px 0 15px;color:#667f97;font-size:10px}.cd-project-list,.cd-doc-list{display:flex;flex-direction:column}.cd-project-row,.cd-doc-row{display:grid;grid-template-columns:34px minmax(0,1fr) auto auto;align-items:center;gap:11px;padding:12px 0;border-top:1px solid #142638}.cd-project-row:first-child,.cd-doc-row:first-child{border-top:0}.cd-project-icon,.cd-doc-icon{width:32px;height:32px;border:1px solid #263f55;border-radius:9px;background:#081725;display:grid;place-items:center;color:#779bbb}.cd-project-info,.cd-doc-info{min-width:0}.cd-project-info strong,.cd-doc-info strong{display:block;color:#dce7ef;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cd-project-info span,.cd-doc-info span{display:block;margin-top:4px;color:#607a92;font-size:9px}.cd-row-link,.cd-text-link{display:inline-flex;align-items:center;gap:6px;color:#8aa9c1;text-decoration:none;font-size:9px;font-weight:650}.cd-row-link:hover,.cd-text-link:hover{color:#c0d5e5}.cd-remove{border:0;background:transparent;color:#637c92;font:600 9px inherit;cursor:pointer;padding:7px}.cd-remove:hover{color:#cf9ca4}.cd-remove:disabled{opacity:.45;cursor:wait}.cd-expiry{font-size:9px;padding:5px 7px;border-radius:999px;white-space:nowrap}.cd-expiry.valid{border:1px solid #284b65;background:#091c2d;color:#83afd2}.cd-expiry.soon{border:1px solid #5b4b25;background:#241f0d;color:#e3c56e}.cd-expiry.expired{border:1px solid #63363d;background:#27151a;color:#e39ba5}.cd-side-title{margin:0 0 14px!important}.cd-contact-list{display:flex;flex-direction:column;gap:12px}.cd-contact-list div{display:flex;align-items:center;gap:9px;color:#a4bacd;font-size:10px;overflow-wrap:anywhere}.cd-contact-list svg{color:#6e91ad;flex:none}.cd-muted{color:#617b92!important}.cd-readiness-score{font-size:42px;font-weight:760;letter-spacing:-.05em;color:#e6eef5;margin:6px 0 4px}.cd-side-copy{margin:0 0 16px;color:#667f97;font-size:10px;line-height:1.6}.cd-wide-link{display:flex;align-items:center;justify-content:space-between;border-top:1px solid #172a3b;padding-top:13px;color:#8baac2;text-decoration:none;font-size:10px;font-weight:650}.cd-wide-link:hover{color:#c1d6e5}.contractor-detail-state{min-height:60vh;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:9px;color:#6b849c;font-size:12px}.contractor-detail-state strong{color:#dbe5ed;font-size:15px}.contractor-detail-state p{margin:0}.contractor-detail-spinner{width:16px;height:16px;border:2px solid #20394f;border-top-color:#78a9d1;border-radius:50%;animation:cd-spin .8s linear infinite}@keyframes cd-spin{to{transform:rotate(360deg)}}.cd-modal-backdrop{position:fixed;inset:0;background:rgba(1,6,11,.72);backdrop-filter:blur(8px);display:grid;place-items:center;padding:20px;z-index:1000}.cd-modal{width:min(560px,100%);border:1px solid #29445b;border-radius:15px;background:#07111d;box-shadow:0 28px 80px rgba(0,0,0,.5);overflow:hidden}.cd-modal-head{padding:22px 24px 18px;border-bottom:1px solid #1b3042;display:flex;justify-content:space-between;gap:20px}.cd-modal h2{margin:0;color:#edf3f9;font-size:22px;letter-spacing:-.025em}.cd-modal p{margin:8px 0 0;color:#6c859d;font-size:11px}.cd-close{width:34px;height:34px;border:1px solid #29445b;border-radius:9px;background:#091827;color:#7d96ad;display:grid;place-items:center;cursor:pointer}.cd-picker-list{max-height:390px;overflow:auto}.cd-picker-row{width:100%;display:grid;grid-template-columns:32px minmax(0,1fr) 20px;align-items:center;gap:11px;padding:13px 20px;border:0;border-bottom:1px solid #142638;background:transparent;color:inherit;text-align:left;cursor:pointer}.cd-picker-row:hover{background:#0a1723}.cd-picker-row:disabled{opacity:.5;cursor:wait}.cd-picker-row strong{display:block;color:#dce7ef;font:650 11px/1.3 inherit}.cd-picker-row small{display:block;margin-top:4px;color:#607a92;font-size:9px}.cd-picker-arrow{color:#66839d}.cd-picker-empty{min-height:180px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:24px;color:#667f97;font-size:10px}.cd-picker-empty strong{color:#dce6ef;font-size:12px;margin-bottom:6px}@media(max-width:900px){.cd-summary-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.cd-grid{grid-template-columns:1fr}.cd-sidebar{display:grid;grid-template-columns:repeat(2,minmax(0,1fr))}.cd-sidebar .cd-panel{min-height:170px}}@media(max-width:650px){.contractor-detail-page{padding:26px 16px 42px}.cd-header{align-items:flex-start;flex-direction:column}.cd-header h1{font-size:30px}.cd-summary-grid{grid-template-columns:1fr 1fr}.cd-summary-grid article{min-height:92px;padding:13px}.cd-summary-grid strong{font-size:21px}.cd-sidebar{display:flex}.cd-panel{padding:16px}.cd-panel-head{flex-direction:column}.cd-project-row{grid-template-columns:32px minmax(0,1fr) auto}.cd-project-row .cd-remove{grid-column:2}.cd-doc-row{grid-template-columns:32px minmax(0,1fr)}.cd-expiry{grid-column:2;justify-self:start}.cd-breadcrumb{margin-bottom:17px}}
`;
