import { useEffect, useMemo, useState } from "react";
import { useSearchParams } from "react-router-dom";
import { api } from "../lib/api";

type Contractor = { id: string; name: string; email: string | null; phone: string | null; status: string };
type Requirement = { id: string; name: string; description: string | null };
type Document = { id: string; contractor_id: string | null; name: string; document_type: string; expires_at: string | null; storage_key: string | null; status: string; created_at: string };
type Match = { id: string; document_id: string; requirement_id: string; created_at: string };
type DocumentDetail = { document: Document; contractor_id: string | null; contractor_name: string | null; matches: Match[] };
type Intelligence = { id: string; document_id: string; owner_user_id: string | null; verification_status: "unverified" | "verified" | "rejected"; review_status: "pending" | "in_review" | "approved" | "rejected"; source_type: "manual" | "upload" | "import" | "integration"; confidence: number | null; issue_date: string | null; rejection_reason: string | null; reviewed_by_user_id: string | null; reviewed_at: string | null; mapping_status: "mapped" | "unmapped"; computed_state: "inactive" | "unverified" | "rejected" | "expired" | "expiring" | "unmapped" | "requires_review" | "valid" };
type Impact = { document_id: string; document_name: string; computed_state: string; requirement_impacts: { requirement_id: string; requirement_name: string; project_count: number; project_ids: string[] }[]; project_impacts: { project_id: string; project_name: string; contractor_id: string; contractor_name: string; readiness_score: number; readiness_status: string; counts_for_readiness: boolean; affected_requirement_ids: string[]; affected_requirement_names: string[] }[] };

type EvidenceState = Intelligence["computed_state"];

function Icon({ name, size = 18 }: { name: "search" | "plus" | "file" | "arrow" | "close" | "check" | "link" | "alert" | "trash" | "shield"; size?: number }) {
  const c = { width: size, height: size, viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 1.7, strokeLinecap: "round" as const, strokeLinejoin: "round" as const };
  if (name === "search") return <svg {...c}><circle cx="11" cy="11" r="6.5"/><path d="m16 16 4.5 4.5"/></svg>;
  if (name === "plus") return <svg {...c}><path d="M12 5v14"/><path d="M5 12h14"/></svg>;
  if (name === "file") return <svg {...c}><path d="M6 3h8l4 4v14H6z"/><path d="M14 3v5h5"/><path d="M9 13h6"/></svg>;
  if (name === "arrow") return <svg {...c}><path d="M5 12h13"/><path d="m13 6 6 6-6 6"/></svg>;
  if (name === "check") return <svg {...c}><path d="m5 12 4 4L19 6"/></svg>;
  if (name === "link") return <svg {...c}><path d="M10 13a5 5 0 0 0 7.5.4l1.5-1.5a5 5 0 0 0-7.1-7.1l-.9.9"/><path d="M14 11a5 5 0 0 0-7.5-.4L5 12.1a5 5 0 0 0 7.1 7.1l.9-.9"/></svg>;
  if (name === "alert") return <svg {...c}><path d="m12 4 9 16H3L12 4Z"/><path d="M12 9v4"/><path d="M12 16h.01"/></svg>;
  if (name === "trash") return <svg {...c}><path d="M4 7h16"/><path d="M9 7V4h6v3"/><path d="M7 7l1 14h8l1-14"/><path d="M10 11v6"/><path d="M14 11v6"/></svg>;
  if (name === "shield") return <svg {...c}><path d="M12 3 19 6v5c0 5-3 8-7 10-4-2-7-5-7-10V6l7-3Z"/><path d="m9 12 2 2 4-4"/></svg>;
  return <svg {...c}><path d="m7 7 10 10"/><path d="m17 7-10 10"/></svg>;
}

function dateLabel(value: string | null) { if (!value) return "No expiry"; const date = new Date(value); return Number.isNaN(date.getTime()) ? "Unknown" : date.toLocaleDateString(undefined, { day: "numeric", month: "short", year: "numeric" }); }
function toIsoDate(value: string) { return value ? new Date(`${value}T23:59:59`).toISOString() : null; }
function stateLabel(value: EvidenceState) {
  return ({ valid: "Valid", expiring: "Expiring soon", expired: "Expired", unmapped: "Unmapped", inactive: "Inactive", unverified: "Unverified", rejected: "Rejected", requires_review: "Requires review" } as Record<string, string>)[value] ?? value;
}
function stateClass(value: string) { return value.replace("_", "-"); }

export default function EvidencePage() {
  const [searchParams] = useSearchParams();
  const [documents, setDocuments] = useState<Document[]>([]);
  const [contractors, setContractors] = useState<Contractor[]>([]);
  const [requirements, setRequirements] = useState<Requirement[]>([]);
  const [details, setDetails] = useState<Record<string, DocumentDetail>>({});
  const [intelligence, setIntelligence] = useState<Record<string, Intelligence>>({});
  const [impacts, setImpacts] = useState<Record<string, Impact>>({});
  const [query, setQuery] = useState(searchParams.get("q") ?? "");
  const [filter, setFilter] = useState("all");
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [busyId, setBusyId] = useState("");
  const [error, setError] = useState("");
  const [showCreate, setShowCreate] = useState(false);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [name, setName] = useState("");
  const [documentType, setDocumentType] = useState("");
  const [contractorId, setContractorId] = useState("");
  const [expiresAt, setExpiresAt] = useState("");
  const [rejectionReason, setRejectionReason] = useState("");

  async function load() {
    try {
      setLoading(true); setError("");
      const [documentList, contractorList, requirementList] = await Promise.all([api<Document[]>("/api/documents"), api<Contractor[]>("/api/contractors"), api<Requirement[]>("/api/requirements")]);
      setDocuments(documentList); setContractors(contractorList); setRequirements(requirementList);
      const entries = await Promise.all(documentList.map(async (document) => {
        try {
          const [detail, intel, impact] = await Promise.all([api<DocumentDetail>(`/api/documents/${document.id}/detail`), api<Intelligence>(`/api/evidence/${document.id}/intelligence`), api<Impact>(`/api/evidence/${document.id}/impact`)]);
          return [document.id, { detail, intel, impact }] as const;
        } catch { return null; }
      }));
      const valid = entries.filter((entry): entry is readonly [string, { detail: DocumentDetail; intel: Intelligence; impact: Impact }] => entry !== null);
      setDetails(Object.fromEntries(valid.map(([id, value]) => [id, value.detail])));
      setIntelligence(Object.fromEntries(valid.map(([id, value]) => [id, value.intel])));
      setImpacts(Object.fromEntries(valid.map(([id, value]) => [id, value.impact])));
    } catch (err) { setError(err instanceof Error ? err.message : "Unable to load evidence."); }
    finally { setLoading(false); }
  }
  useEffect(() => { void load(); }, []);

  const contractorNames = useMemo(() => Object.fromEntries(contractors.map((item) => [item.id, item.name])), [contractors]);
  const requirementNames = useMemo(() => Object.fromEntries(requirements.map((item) => [item.id, item.name])), [requirements]);
  const filtered = useMemo(() => { const value = query.trim().toLowerCase(); return documents.filter((document) => { const intel = intelligence[document.id]; const state = intel?.computed_state ?? "unverified"; const matchesQuery = !value || `${document.name} ${document.document_type} ${contractorNames[document.contractor_id || ""] || ""}`.toLowerCase().includes(value); const matchesFilter = filter === "all" || state === filter; return matchesQuery && matchesFilter; }); }, [documents, intelligence, query, filter, contractorNames]);
  const counts = useMemo(() => documents.reduce((acc, document) => { const state = intelligence[document.id]?.computed_state ?? "unverified"; if (state in acc) acc[state as keyof typeof acc] += 1; return acc; }, { valid: 0, expiring: 0, expired: 0, unmapped: 0, inactive: 0, unverified: 0, rejected: 0, requires_review: 0 }), [documents, intelligence]);

  async function createEvidence(event: React.FormEvent) { event.preventDefault(); if (!name.trim() || !documentType.trim() || !contractorId) return; try { setSaving(true); setError(""); await api("/api/documents", { method: "POST", body: JSON.stringify({ contractor_id: contractorId, name: name.trim(), document_type: documentType.trim(), expires_at: toIsoDate(expiresAt) }) }); setName(""); setDocumentType(""); setContractorId(""); setExpiresAt(""); setShowCreate(false); await load(); } catch (err) { setError(err instanceof Error ? err.message : "Unable to create evidence."); } finally { setSaving(false); } }
  async function updateSelected(patch: Partial<Document>) { if (!selectedId) return; try { setBusyId(selectedId); await api(`/api/documents/${selectedId}`, { method: "PUT", body: JSON.stringify(patch) }); await load(); setSelectedId(null); } catch (err) { setError(err instanceof Error ? err.message : "Unable to update evidence."); } finally { setBusyId(""); } }
  async function updateIntelligence(patch: Partial<Intelligence>) { if (!selectedId) return; if (patch.verification_status === "rejected" && !rejectionReason.trim()) { setError("A rejection reason is required."); return; } try { setBusyId(`intel:${selectedId}`); setError(""); const updated = await api<Intelligence>(`/api/evidence/${selectedId}/intelligence`, { method: "PATCH", body: JSON.stringify({ ...patch, ...(patch.verification_status === "rejected" ? { rejection_reason: rejectionReason.trim() } : {}) }) }); setIntelligence((current) => ({ ...current, [selectedId]: updated })); setRejectionReason(""); const impact = await api<Impact>(`/api/evidence/${selectedId}/impact`); setImpacts((current) => ({ ...current, [selectedId]: impact })); } catch (err) { setError(err instanceof Error ? err.message : "Unable to update evidence intelligence."); } finally { setBusyId(""); } }
  async function deleteSelected() { if (!selectedId) return; const target = documents.find((item) => item.id === selectedId); if (!target || !window.confirm(`Delete “${target.name}”? This cannot be undone.`)) return; try { setBusyId(selectedId); await api(`/api/documents/${selectedId}`, { method: "DELETE" }); setSelectedId(null); await load(); } catch (err) { setError(err instanceof Error ? err.message : "Unable to delete evidence."); } finally { setBusyId(""); } }
  async function mapRequirement(requirementId: string) { if (!selectedId) return; try { setBusyId(requirementId); await api(`/api/documents/${selectedId}/requirements/${requirementId}`, { method: "POST" }); await load(); } catch (err) { setError(err instanceof Error ? err.message : "Unable to map evidence."); } finally { setBusyId(""); } }
  async function unmapRequirement(requirementId: string) { if (!selectedId) return; try { setBusyId(requirementId); await api(`/api/documents/${selectedId}/requirements/${requirementId}`, { method: "DELETE" }); await load(); } catch (err) { setError(err instanceof Error ? err.message : "Unable to remove evidence mapping."); } finally { setBusyId(""); } }

  const selected = selectedId ? details[selectedId] : null;
  const selectedDocument = selected?.document;
  const selectedIntel = selectedId ? intelligence[selectedId] : null;
  const selectedImpact = selectedId ? impacts[selectedId] : null;
  const mappedRequirementIds = new Set(selected?.matches.map((match) => match.requirement_id) ?? []);

  return <><style>{styles}</style><section className="evidence-page">
    <header className="evidence-header"><div><span className="evidence-eyebrow">WORK · EVIDENCE</span><h1>Evidence</h1><p>Evidence is evaluated, mapped, and connected directly to contractor readiness.</p></div><button className="evidence-primary" type="button" onClick={() => setShowCreate(true)}><Icon name="plus" size={15}/> Add evidence</button></header>
    {error && <div className="evidence-error"><Icon name="alert" size={15}/><span>{error}</span></div>}
    <section className="evidence-stats">
      {["all","valid","unverified","requires_review","rejected","expiring","expired","unmapped"].map((state) => <button key={state} className={`evidence-stat ${filter === state ? "selected" : ""}`} type="button" onClick={() => setFilter(state)}><span>{state === "all" ? "Total evidence" : stateLabel(state)}</span><strong>{state === "all" ? documents.length : counts[state as keyof typeof counts]}</strong></button>)}
    </section>
    <section className="evidence-toolbar"><label className="evidence-search"><Icon name="search" size={17}/><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search evidence, type, or contractor"/></label><span className="evidence-count">{filtered.length} {filtered.length === 1 ? "record" : "records"}</span></section>
    <section className="evidence-panel"><div className="evidence-head"><span>Evidence</span><span>Contractor</span><span>Expiry</span><span>Mapping</span><span>Intelligence</span><span/></div>
      {loading ? <div className="evidence-state"><div className="evidence-spinner"/><span>Loading evidence intelligence…</span></div> : filtered.length === 0 ? <div className="evidence-empty"><div className="evidence-empty-icon"><Icon name="file" size={22}/></div><strong>{documents.length ? "No matching evidence" : "No evidence yet"}</strong><span>{documents.length ? "Try another filter." : "Add evidence to a contractor before mapping it to requirements."}</span></div> : <div>{filtered.map((document) => { const detail = details[document.id]; const intel = intelligence[document.id]; const state = intel?.computed_state ?? "unverified"; return <button className="evidence-row" key={document.id} type="button" onClick={() => setSelectedId(document.id)}><div className="evidence-main"><div className="evidence-icon"><Icon name="file" size={15}/></div><div><strong>{document.name}</strong><span>{document.document_type}</span></div></div><div className="evidence-contractor">{contractorNames[document.contractor_id || ""] || "Unassigned"}</div><div className="evidence-expiry">{dateLabel(document.expires_at)}</div><div className="evidence-mapping">{detail?.matches.length ? <><Icon name="link" size={13}/><span>{detail.matches.length} mapped</span></> : <span>Not mapped</span>}</div><div><span className={`evidence-state-chip ${stateClass(state)}`}>{stateLabel(state)}</span></div><div className="evidence-arrow"><Icon name="arrow" size={14}/></div></button>; })}</div>}
    </section>
    <section className="evidence-footer"><div><span className="evidence-eyebrow">THE AGATA EVIDENCE MODEL</span><h2>Evidence → Requirement → Project → Readiness</h2><p>AGATA now exposes the actual downstream impact of an evidence record, while the deterministic readiness engine remains the source of truth.</p></div><div className="evidence-flow"><span>Evidence</span><Icon name="arrow" size={13}/><span>Requirement</span><Icon name="arrow" size={13}/><span>Project</span><Icon name="arrow" size={13}/><span>Readiness</span></div></section>
  </section>

  {showCreate && <div className="evidence-modal-backdrop" onMouseDown={(e) => { if (e.currentTarget === e.target) setShowCreate(false); }}><section className="evidence-modal"><div className="evidence-modal-head"><div><span className="evidence-eyebrow">NEW EVIDENCE</span><h2>Add an evidence record</h2><p>New evidence starts unverified and cannot satisfy readiness until it is verified and approved.</p></div><button className="evidence-close" type="button" onClick={() => setShowCreate(false)}><Icon name="close" size={17}/></button></div><form onSubmit={createEvidence}><label>Contractor<select value={contractorId} onChange={(e) => setContractorId(e.target.value)} required><option value="">Select contractor</option>{contractors.map((contractor) => <option key={contractor.id} value={contractor.id}>{contractor.name}</option>)}</select></label><label>Evidence name<input autoFocus value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Insurance Certificate" required/></label><label>Evidence type<input value={documentType} onChange={(e) => setDocumentType(e.target.value)} placeholder="e.g. Insurance Certificate" required/></label><label>Expiry date <span>Optional</span><input type="date" value={expiresAt} onChange={(e) => setExpiresAt(e.target.value)}/></label><div className="evidence-modal-actions"><button className="evidence-secondary" type="button" onClick={() => setShowCreate(false)}>Cancel</button><button className="evidence-primary" type="submit" disabled={saving}>{saving ? "Creating…" : "Create evidence"}</button></div></form></section></div>}

  {selectedDocument && selectedIntel && <div className="evidence-modal-backdrop" onMouseDown={(e) => { if (e.currentTarget === e.target) setSelectedId(null); }}><section className="evidence-detail-modal"><div className="evidence-modal-head"><div><span className="evidence-eyebrow">EVIDENCE INTELLIGENCE</span><h2>{selectedDocument.name}</h2><p>{selectedDocument.document_type} · {selected?.contractor_name || "Unassigned"}</p></div><button className="evidence-close" type="button" onClick={() => setSelectedId(null)}><Icon name="close" size={17}/></button></div><div className="evidence-detail-body">
    <div className="evidence-detail-grid"><div><span>Computed state</span><strong className={`detail-state ${stateClass(selectedIntel.computed_state)}`}>{stateLabel(selectedIntel.computed_state)}</strong></div><div><span>Verification</span><strong>{selectedIntel.verification_status}</strong></div><div><span>Review</span><strong>{selectedIntel.review_status}</strong></div><div><span>Confidence</span><strong>{selectedIntel.confidence === null ? "Not set" : `${selectedIntel.confidence}%`}</strong></div></div>
    <div className="intel-controls"><div><span className="evidence-eyebrow">VERIFICATION</span><div className="intel-actions"><button type="button" className="evidence-secondary" disabled={busyId.startsWith("intel:")} onClick={() => void updateIntelligence({ verification_status: "verified" })}><Icon name="check" size={13}/> Verify</button><button type="button" className="evidence-danger" disabled={busyId.startsWith("intel:")} onClick={() => void updateIntelligence({ verification_status: "rejected" })}><Icon name="alert" size={13}/> Reject</button></div>{selectedIntel.verification_status === "rejected" && <textarea className="rejection-input" value={rejectionReason} onChange={(e) => setRejectionReason(e.target.value)} placeholder="Reason for rejection"/>}</div><div><span className="evidence-eyebrow">REVIEW</span><div className="intel-actions"><button type="button" className="evidence-secondary" disabled={busyId.startsWith("intel:")} onClick={() => void updateIntelligence({ review_status: "in_review" })}>In review</button><button type="button" className="evidence-primary" disabled={busyId.startsWith("intel:")} onClick={() => void updateIntelligence({ review_status: "approved" })}>Approve</button></div></div></div>
    <div className="evidence-detail-section"><span className="evidence-eyebrow">READINESS IMPACT</span><h3>Where this evidence can affect decisions</h3>{selectedImpact?.project_impacts.length ? <div className="impact-list">{selectedImpact.project_impacts.map((project) => <div className="impact-item" key={project.project_id}><div><strong>{project.project_name}</strong><span>{project.contractor_name} · {project.affected_requirement_names.join(", ") || "No affected requirement"}</span></div><div className="impact-score"><strong>{project.readiness_score}%</strong><span className={`impact-status ${project.readiness_status}`}>{project.readiness_status.replace("_", " ")}</span></div></div>)}</div> : <div className="mapping-empty"><Icon name="shield" size={16}/><span>No assigned project is currently affected by this evidence.</span></div>}</div>
    <div className="evidence-detail-section"><div className="evidence-detail-section-head"><span className="evidence-eyebrow">MAPPING</span><h3>Requirements this evidence supports</h3></div>{selected.matches.length ? <div className="mapped-list">{selected.matches.map((match) => <div className="mapped-item" key={match.id}><div><Icon name="check" size={14}/><span>{requirementNames[match.requirement_id] || "Requirement"}</span></div><button type="button" disabled={busyId === match.requirement_id} onClick={() => void unmapRequirement(match.requirement_id)}>Remove</button></div>)}</div> : <div className="mapping-empty"><Icon name="link" size={16}/><span>This evidence is not mapped to a requirement yet.</span></div>}</div>
    <div className="evidence-detail-section"><span className="evidence-eyebrow">AVAILABLE REQUIREMENTS</span><div className="available-list">{requirements.filter((requirement) => !mappedRequirementIds.has(requirement.id)).map((requirement) => <button className="available-item" key={requirement.id} type="button" disabled={busyId === requirement.id} onClick={() => void mapRequirement(requirement.id)}><span>{requirement.name}</span><Icon name="plus" size={14}/></button>)}</div></div>
    <div className="evidence-detail-actions"><button className="evidence-secondary" type="button" disabled={busyId === selectedDocument.id} onClick={() => void updateSelected({ status: selectedDocument.status === "active" ? "inactive" : "active" })}>{selectedDocument.status === "active" ? "Mark inactive" : "Reactivate"}</button><button className="evidence-danger" type="button" disabled={busyId === selectedDocument.id} onClick={() => void deleteSelected()}><Icon name="trash" size={14}/> Delete evidence</button></div>
  </div></section></div>}
  </>;
}

const styles = `
.evidence-page{max-width:1180px;margin:0 auto;padding:40px 30px 60px;color:#e8eef7}.evidence-header{display:flex;align-items:flex-end;justify-content:space-between;gap:28px;margin-bottom:25px}.evidence-eyebrow{display:block;font-size:9px;letter-spacing:.19em;font-weight:700;color:#7f9bb8;margin-bottom:8px}.evidence-header h1{margin:0;font-size:44px;line-height:1;font-weight:760;letter-spacing:-.045em}.evidence-header p{margin:12px 0 0;color:#7890a9;font-size:13px;max-width:680px}.evidence-primary,.evidence-secondary,.evidence-danger{border:1px solid #29455e;border-radius:10px;background:#0b1d2e;color:#dce9f7;font:600 12px/1 inherit;padding:12px 15px;display:inline-flex;align-items:center;justify-content:center;gap:8px;cursor:pointer;transition:.18s ease}.evidence-primary:hover{border-color:#3c6b91;background:#10283d}.evidence-primary:disabled,.evidence-secondary:disabled,.evidence-danger:disabled{opacity:.55;cursor:not-allowed}.evidence-secondary{background:transparent;color:#a9bdd0}.evidence-danger{border-color:#63363d;background:#27151a;color:#e39ba5}.evidence-error{display:flex;align-items:center;gap:8px;border:1px solid #6b3941;background:#29171c;color:#e9b5bc;border-radius:10px;padding:11px 13px;margin-bottom:15px;font-size:12px}.evidence-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:9px;margin-bottom:13px}.evidence-stat{min-height:72px;text-align:left;border:1px solid #182d41;border-radius:12px;background:#07111b;padding:13px;color:inherit;cursor:pointer}.evidence-stat:hover,.evidence-stat.selected{border-color:#315a7c;background:#0a1825}.evidence-stat span{display:block;color:#627a92;font-size:9px}.evidence-stat strong{display:block;margin-top:7px;font-size:22px;letter-spacing:-.04em}.evidence-toolbar{display:flex;align-items:center;justify-content:space-between;gap:15px;margin-bottom:12px}.evidence-search{width:min(500px,100%);height:42px;display:flex;align-items:center;gap:10px;padding:0 13px;border:1px solid #1d3449;border-radius:10px;background:#07111d;color:#607b94}.evidence-search input{border:0;outline:0;background:transparent;color:#dce8f4;width:100%;font:500 12px/1.2 inherit}.evidence-search input::placeholder{color:#526b83}.evidence-count{font-size:11px;color:#637c94}.evidence-panel{border:1px solid #1b3044;border-radius:14px;background:#050b13;overflow:hidden}.evidence-head,.evidence-row{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(120px,1fr) 110px 110px 115px 25px;align-items:center;column-gap:13px}.evidence-head{min-height:46px;padding:0 18px;border-bottom:1px solid #192c3e;color:#5f7890;font-size:9px;letter-spacing:.14em;text-transform:uppercase;font-weight:700}.evidence-row{width:100%;box-sizing:border-box;min-height:79px;padding:12px 18px;border:0;border-bottom:1px solid #142638;background:transparent;color:inherit;text-align:left;font:inherit;cursor:pointer}.evidence-row:hover{background:#08131f}.evidence-main{display:flex;align-items:center;gap:10px;min-width:0}.evidence-icon{width:33px;height:33px;display:grid;place-items:center;flex:0 0 33px;border:1px solid #28445c;border-radius:9px;background:#0c1824;color:#79b5e3}.evidence-main>div:last-child{min-width:0}.evidence-main strong{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#dfe8f1;font-size:11px}.evidence-main span{display:block;margin-top:4px;color:#617a92;font-size:8px}.evidence-contractor,.evidence-expiry{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:#8ca2b7;font-size:9px}.evidence-mapping{display:flex;align-items:center;gap:6px;color:#6f91ad;font-size:8px}.evidence-state-chip{display:inline-flex;padding:5px 8px;border-radius:999px;font-size:8px;font-weight:700;border:1px solid #29455e;background:#0a1723;color:#9db4c8}.evidence-state-chip.valid{color:#71d6a3;background:rgba(48,129,91,.1);border-color:rgba(113,214,163,.18)}.evidence-state-chip.expiring{color:#d9b16a;background:rgba(126,89,28,.1)}.evidence-state-chip.expired,.evidence-state-chip.rejected{color:#df8989;background:rgba(116,35,35,.1)}.evidence-state-chip.unmapped{color:#8abfe6;background:rgba(50,104,145,.1)}.evidence-state-chip.unverified{color:#a9b9c8}.evidence-state-chip.requires-review{color:#d7b3e7;background:rgba(110,65,135,.12)}.evidence-arrow{display:grid;place-items:center;color:#47637b}.evidence-state,.evidence-empty{min-height:300px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;text-align:center;color:#61758a;font-size:10px}.evidence-empty-icon{width:48px;height:48px;display:grid;place-items:center;margin-bottom:6px;border:1px solid #23415b;border-radius:12px;background:#0d1b29;color:#79b8ef}.evidence-empty strong{color:#dce7f0;font-size:12px}.evidence-empty span{max-width:430px;color:#5d7185;font-size:9px;line-height:1.6}.evidence-spinner{width:25px;height:25px;border:2px solid #1c3449;border-top-color:#76b9ef;border-radius:50%;animation:evidence-spin .8s linear infinite}@keyframes evidence-spin{to{transform:rotate(360deg)}}.evidence-footer{display:flex;align-items:center;justify-content:space-between;gap:35px;margin-top:14px;padding:23px;border:1px solid #172b3e;border-radius:13px;background:#08111b}.evidence-footer h2{margin:0 0 7px;font-size:16px;letter-spacing:-.025em}.evidence-footer p{max-width:650px;margin:0;color:#60758a;font-size:9px;line-height:1.7}.evidence-flow{display:flex;align-items:center;gap:9px;color:#7592aa;font-size:8px;white-space:nowrap}.evidence-modal-backdrop{position:fixed;inset:0;z-index:100;display:grid;place-items:center;padding:24px;background:rgba(2,6,10,.74);backdrop-filter:blur(8px)}.evidence-modal,.evidence-detail-modal{width:min(540px,100%);max-height:min(760px,90vh);overflow:auto;border:1px solid #24384b;border-radius:15px;background:#0a1119;box-shadow:0 35px 100px rgba(0,0,0,.48)}.evidence-detail-modal{width:min(760px,100%)}.evidence-modal-head{display:flex;justify-content:space-between;gap:18px;padding:22px;border-bottom:1px solid #172532}.evidence-modal-head h2{margin:7px 0 5px;font-size:20px;letter-spacing:-.025em}.evidence-modal-head p{margin:0;color:#63768a;font-size:9px;line-height:1.6}.evidence-close{width:31px;height:31px;display:grid;place-items:center;flex:0 0 31px;border:1px solid #1d3041;border-radius:8px;background:#0c141d;color:#6b8094;cursor:pointer}.evidence-modal form{display:grid;gap:15px;padding:21px}.evidence-modal label{display:grid;gap:7px;color:#a6b9cb;font-size:9px;font-weight:700}.evidence-modal label span{color:#5f7890;font-weight:500}.evidence-modal input,.evidence-modal select{height:41px;box-sizing:border-box;border:1px solid #1d3449;border-radius:9px;background:#07111b;padding:0 11px;color:#dce8f4}.evidence-modal select option{background:#0a1119}.evidence-modal-actions,.evidence-detail-actions{display:flex;justify-content:flex-end;gap:9px;margin-top:5px}.evidence-detail-body{padding:21px}.evidence-detail-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.evidence-detail-grid>div{min-height:68px;padding:12px;border:1px solid #172b3e;border-radius:10px;background:#07111a}.evidence-detail-grid span{display:block;color:#5f7890;font-size:8px}.evidence-detail-grid strong{display:block;margin-top:7px;color:#dce8f2;font-size:10px;text-transform:capitalize}.detail-state.valid{color:#71d6a3}.detail-state.rejected,.detail-state.expired{color:#df8989}.detail-state.unverified{color:#a9b9c8}.detail-state.requires-review{color:#d7b3e7}.intel-controls{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:20px;padding:15px;border:1px solid #172b3e;border-radius:11px;background:#07111a}.intel-actions{display:flex;gap:8px;margin-top:8px}.rejection-input{width:100%;min-height:68px;box-sizing:border-box;margin-top:9px;padding:10px;border:1px solid #51333a;border-radius:8px;background:#0a1119;color:#dce8f4;resize:vertical;font:500 11px/1.5 inherit}.evidence-detail-section{margin-top:20px}.evidence-detail-section h3{margin:6px 0 11px;font-size:13px}.impact-list,.mapped-list,.available-list{display:grid;gap:6px}.impact-item,.mapped-item,.available-item{display:flex;align-items:center;justify-content:space-between;gap:12px;min-height:44px;padding:0 11px;border:1px solid #172b3e;border-radius:9px;background:#07111a;color:#c7d6e2}.impact-item>div:first-child{min-width:0}.impact-item strong{display:block;font-size:10px}.impact-item span{display:block;margin-top:4px;color:#647b91;font-size:8px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.impact-score{text-align:right;flex:0 0 auto}.impact-score strong{font-size:13px}.impact-status{font-size:8px!important;text-transform:capitalize}.impact-status.ready{color:#71d6a3}.impact-status.attention{color:#d9b16a}.impact-status.not_ready{color:#df8989}.mapped-item>div{display:flex;align-items:center;gap:8px;font-size:9px}.mapped-item svg{color:#69ca9b}.mapped-item button{border:0;background:transparent;color:#718ba1;font-size:8px;cursor:pointer}.available-item{width:100%;text-align:left;font:inherit;cursor:pointer}.available-item:hover{border-color:#2d4e69}.mapping-empty{display:flex;align-items:center;gap:8px;padding:14px;border:1px dashed #203547;border-radius:9px;color:#60758a;font-size:9px}.evidence-detail-actions{justify-content:space-between;margin-top:25px;padding-top:17px;border-top:1px solid #172b3e}.evidence-detail-actions .evidence-secondary{margin-right:auto}
@media(max-width:900px){.evidence-page{padding:30px 18px 50px}.evidence-stats{grid-template-columns:repeat(3,1fr)}.evidence-head,.evidence-row{grid-template-columns:minmax(0,1.5fr) minmax(120px,1fr) 100px 110px 105px 20px}.evidence-footer{align-items:flex-start;flex-direction:column}.evidence-detail-grid{grid-template-columns:repeat(2,1fr)}.intel-controls{grid-template-columns:1fr}}
@media(max-width:650px){.evidence-header{align-items:flex-start;flex-direction:column}.evidence-primary{width:100%}.evidence-stats{grid-template-columns:repeat(2,1fr)}.evidence-head{display:none}.evidence-row{grid-template-columns:minmax(0,1fr) auto;gap:8px;padding:13px 14px}.evidence-row>.evidence-contractor,.evidence-row>.evidence-expiry,.evidence-row>.evidence-mapping{display:none}.evidence-row>div:nth-child(5){justify-self:end}.evidence-arrow{grid-column:2;grid-row:1}.evidence-detail-grid{grid-template-columns:1fr}.evidence-detail-actions{align-items:stretch;flex-direction:column}.evidence-detail-actions .evidence-secondary{margin-right:0}.evidence-modal-backdrop{padding:12px}.evidence-footer{padding:18px}.evidence-flow{white-space:normal}}
`;
